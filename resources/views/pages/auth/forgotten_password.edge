<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      Mot de passe oublié - Relaxes Pour Le Vivant
    </title>
    @include('partials/bootstrap')
  </head>

  <body>
    <header>
      @include('partials/navbar')
    </header>

    <div class="container my-5">
      <div class="row justify-content-center">
        <div class="col-md-4">
          <div class="vstack gap-3">
            <img
              class="d-block mx-auto"
              style="width: 48px;"
              src="{{ asset('resources/images/msde_icon.svg') }}"
              alt="MSDE logo icon"
            />
            <h2 class="text-center">
              Mot de passe oublié
            </h2>
            <p class="text-center text-secondary">
              Entrez l'adresse e-mail de votre compte pour demander une réinitialisation du mot de passe.
            </p>
            <form method="POST" action="/forgotten-password">
              <div class="vstack gap-3">
                {{ csrfField() }}
                @if(flashMessages.has('errors.password_reset'))
                  <div class="alert alert-danger">
                    {{ flashMessages.get('errors.password_reset') }}
                  </div>
                @endif
                @if(flashMessages.has('success.forgotten_password'))
                  <div class="alert alert-success">
                    {{ flashMessages.get('success.forgotten_password') }}
                  </div>
                @endif
                <div class="mb-3">
                  <label for="email" class="form-label text-secondary">Email</label>
                  <input
                    type="email"
                    class="form-control"
                    id="email"
                    name="email"
                    placeholder="name@example.com"
                    required
                  />
                </div>
                <div class="d-grid">
                  <button type="submit" class="btn btn-primary">Réinitialiser mon mot de passe</button>
                </div>
              </div>
            </form>
            <div class="mt-3 text-center">
              <p class="text-secondary">
                Vous n’avez pas de compte? <a href="/sign-up">Demander un accès</a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>

</html>
