<div class="mb-3">
  <div
    class="token-input-wrapper"
    data-token-select
    data-options="{{ JSON.stringify(options) }}"
    data-selected="{{ JSON.stringify(selected) }}"
    data-name="{{ name }}"
  >
    <input type="text" class="token-input" placeholder="{{ label }}" autocomplete="off" />
    <div class="token-dropdown">
    </div>
  </div>
</div>
